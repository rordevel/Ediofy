= render 'university_header'

.content

  .user-collections-list
    .items-header
      %h2= t '.title'
    - if @user_collections.any?
      %ol.collection-items= render @user_collections
      = paginate @user_collections
    - else
      .no-items= t '.no_items'